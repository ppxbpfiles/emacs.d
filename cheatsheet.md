# Emacs キーバインド チートシート

## CUA モード（Windows 互換）

| キー | 動作 |
|---|---|
| `C-c` | コピー |
| `C-x` | 切り取り |
| `C-v` | 貼り付け |
| `C-z` | アンドゥ |
| `C-y` | リドゥ |
| `C-s` | 上書き保存 |
| `C-a` | 全選択 |
| `C-o` | ファイルを開く（Windows ダイアログ） |
| `C-w` | バッファ（ファイル）を閉じる |
| `Alt+F4` / `C-x C-c` | Emacs を終了する |
| `M-z` | Undo履歴をツリー表示（vundo） |

---

## Meow（モーダル編集）

NORMAL/INSERT/MOTION等の状態を持つモーダル編集レイヤー。CUAのCtrl修飾キーとは層が違うため共存している。モードラインの `NORMAL`/`INSERT`/`BEACON`/`MOTION`/`KEYPAD` 等で現在の状態を確認できる。

### NORMAL state（抜粋・変更/追加分のみ）

| キー | 動作 |
|---|---|
| `x` | 1行選択（連打で下の行へ選択範囲を拡張、Helix風） |
| `X` | 上の行を選択・拡張（`x` の逆方向） |
| `s` | 選択範囲内の一括マッチ（Beaconマルチ選択、Smart Case対応、Helix風） |
| `r` | ミニバッファ対話式の一括置換（空Enterでコピー内容置換、Helix風） |
| `%` | バッファ全体を選択（Helix風） |
| `C` | 選択行を各行カーソル（Beacon）に分割（Helix風） |
| `~` | 選択範囲の大文字・小文字をトグル反転（Helix風） |
| `g` | Goto 体系（`gh`: 行頭, `gl`: 行末, `gg`: 先頭, `ge`: 末尾, `gi`: インデント行頭, `RET → 50`: 指定行ジャンプ） |
| `w` / `W` | 単語を選択 / シンボル（変数名）を選択 |
| `o` / `O` | 括弧ブロックを選択（連打で外側の親ブロックへ拡張） / ブロック端まで選択 |
| `i` / `a` | 挿入モードへ（カーソル位置 / 右に1つ進めてから） |
| `A` / `I` | 下に新しい行を開いて挿入 / 上に新しい行を開いて挿入（※Vimの `o` / `O` 相当） |
| `d` | 切り取り（選択中はkill-region、無選択時は行末削除。M-0〜9でレジスタ指定可） |
| `y` | コピー（M-0〜9でレジスタ指定可） |
| `p` | 貼り付け（CUAのC-v互換。M-0〜9でレジスタ指定可） |
| `c` | 変更（選択範囲を削除して挿入モードへ、Beacon全カーソル対応） |
| `u` / `U` | Undo（元に戻す） / 選択範囲内のみUndo |
| `<escape>` / `q` | 万能脱出（Beacon・選択範囲を完全クリアしてNORMAL復帰） |
| `<` / `>` | バッファの先頭 / 末尾へ移動 |
| `v` | 検索して選択（そのままd/c/yで編集につなげる用） |
| `/` | `isearch-forward`（Vim風の検索開始。Migemo対応） |
| `?`（Shift+/） | `isearch-backward` |
| `n` / `N` | 次の一致を検索 / 前の一致を逆方向検索（Vim風） |
| `- n` | 逆方向に検索（Meow標準の負の引数操作） |
| `;` | 選択の向きを前後に反転（meow-reverse） |
| `(` / `)` | 式全体を選択（括弧込） / 式の中身だけを選択（Puni構造編集） |
| `m` | 複数行を1行に結合（Join） |
| `f` / `t` <文字> | 指定文字まで移動（選択） / 直前まで移動 |
| `G` / `R` / `Y` | Grab機能: `G` でテキストをキープ → 別テキストを選択して `R` で瞬時に入替え！ (`Y` で上書き) |
| `F1` | スマート操作ガイド（NORMAL / INSERTに応じた図解ヘルプを表示） |

### おすすめ編集フロー（必修テクニック）
- **一括置換**: `x`（行選択）または `%`（全選択） → `s`（検索語） → `r`（置換文字を入力してEnterで全箇所即座に置換！）
- **各行一括入力**: `x`（複数行を選択） → `C`（各行カーソル分割） → `i` または `a` で入力 → `ESC` で確定・脱出
- **括弧の編集**: `)` → `c`（括弧の中身だけ書き換え） / `(` → `d`（括弧ごと丸ごと削除）
- **テキストの入替え**: 単語を選択して `G`（キープ） → 別の単語を選択して `R`（一発で前後を入替え！）

### リーダーキー（SPC）

| キー | 動作 |
|---|---|
| `SPC ?` | チートシート表示 |
| `SPC /` | 現在のキーマップの内容を表示 |
| `SPC b` | ブックマーク一覧（consult-bookmark） |
| `SPC m` | 足跡一覧（現在のファイル）（consult-mark） |
| `SPC M` | 全ファイル足跡一覧（consult-global-mark） |
| `SPC s` | Consult 探索メニュー（全探索ツールを呼び出す） |
| `SPC p (` / `[` / `{` / `<` | 選択範囲を丸 / 角 / 波 / 山括弧で包む（puni-wrap） |
| `SPC p s` | 囲んでいる括弧だけを外す（puni-splice） |
| `SPC p l` / `p b` | 次の要素を括弧内に取り込む / 末尾の要素を外に出す（puni-slurp/barf） |

### 挿入モード

| キー | 動作 |
|---|---|
| `<backspace>` | 構造を壊さない削除（puni-backward-delete-char） |
| `<escape>` | NORMALに戻る（同時にIMEも自動OFF） |
| `C-\` | mozc（日本語入力）ON / OFF 切り替え |

> [!TIP]
> `mozc-modeless` は Windows Terminal 等の `-nw`（ターミナル）環境とも相性が良く、OS の IME 制約を受けずに `Ctrl+\` で手軽に日本語入力を切り替えられます。
> （※ mozc-modeless を使う場合は本家「Google 日本語入力」のインストールが必要ですが、Windows の常用 IME に設定しておく必要はありません）

---

## ファンクションキー

| キー | 動作 |
|---|---|
| `F1` | スマート操作ガイド（NORMAL / INSERTに応じた図解ヘルプを表示） / `S-F1`: Emacs 標準ヘルプ |
| `F2` | バッファ切り替え（consult-buffer）— centaur-tabs で非表示のタブも選択可能 |
| `F3` | インクリメンタル検索開始 / 次を検索（兼用・前方）※Migemo対応(ローマ字で日本語検索可) |
| `S-F3` | インクリメンタル検索開始 / 前を検索（兼用・後方）※Migemo対応 |
| `F4` | アウトラインサイドバー開閉（imenu-list） |
| `F5` | バッファ再読み込み（ディスクから更新確認） |
| `F6` | 電卓（Calc）を開く |
| `F7` | howm 環境トグル（ON: メニュー / OFF: 全バッファ閉じる） / `S-F7`: howm 全文検索 |
| `F8` | カレンダー開閉トグル（calfw / howm 予定連携） / `S-F8`: 週間天気予報 |
| `F9` | IMAP メーラー開閉トグル（auximap）— 押すと開き、もう一度押すと元の画面へ復帰 |
| `Home` | ダッシュボードの表示 / 再描画（ダッシュボード上では閉じて元のバッファに戻る） |

---

## 検索・移動

| キー | 動作 |
|---|---|
| `C-f` | バッファ内ローマ字検索（Migemo） |
| `C-S-f` | ripgrep 全文検索（プロジェクト） |
| `C-S-g` | ファイル名検索（fd） |
| `C-r` | アウトライン検索（consult-outline） |
| `C-c l` | カーソル下の単語をポップアップ検索 |
| `C-c f` | エディター風フィルタ（マッチ行のみ表示・直接編集・一括保存） |
| `C-c F` | 複数ファイルの一括置換（wgrep + consult-ripgrep） |
| `C-c r w` | カーソル下の単語で ripgrep 検索 |
| `C-.` | カーソル位置でアクション（Embark） |
| `C-c b` | 全ブラウザ Web ブックマーク横断検索（consult-auxbookmark） |
| `C-e` | 行頭・行末のスマートトグル（インデント先頭 ⇆ 本当の行頭 ⇆ 行末） |
| `C-x r b` / `SPC b` / `M-o b` | ブックマーク一覧（consult-bookmark） |
| `C-x r m` | 現在のバッファ・位置をブックマーク保存（bookmark-set） |
| `Alt + ←` | バッファ履歴を戻る（ブラウザの「戻る」のようにいくつ前でも過去のファイルへ遡る） |
| `Alt + →` | バッファ履歴を進む（ブラウザの「進む」） |
| `C-'` | ピン留め（目印を打つ）（範囲選択を起こさずに足跡履歴へ登録。どこにも飛ばず何箇所でも打てます） |
| `C-S-'` / `C-u C-'` | ピン全消去（クリア）（このファイルのピン・足跡履歴をすべてクリアしてリセット） |
| `M-g m` / `SPC m` | 足跡一覧（現在のファイル）（consult-mark：過去の移動・編集・ピン留め履歴をリアルタイムプレビュー） |
| `M-g M` / `SPC M` | 全ファイル足跡一覧（consult-global-mark：別ファイルも含めた全足跡履歴一覧） |
| ツールバーの `Consult検索` ボタン | クリック時にメニューを表示し、行検索（consult-line）またはアウトライン検索（consult-outline）を起動 |

---

## Consult 探索メニュー

**`SPC s`**（NORMALモード時）、**`M-s`** または **`C-c s`**（入力中など全モード共通）を押すと、Consult の各種探索機能を集約した専用メニューが開きます。1文字キーを押すだけで目的の機能を呼び出せます。

| メニューキー | 動作・役割 | 正式関数名 |
|---|---|---|
| `b` | 全バッファ・履歴・ブックマーク一覧 | `consult-buffer` |
| `f` | 最近開いたファイル履歴 | `consult-recent-file` |
| `g` | プロジェクト内のファイル名検索（fd） | `my/consult-fd-project` (`consult-fd`) |
| `G` | カレントフォルダ起点のファイル名検索（fd） | `my/consult-fd-here` |
| `e` | PC全体の超高速ファイル検索（Everything） | `consult-locate` |
| `l` | バッファ内行検索（ローマ字・日本語Migemo対応） | `my/consult-line-migemo` (`consult-line`) |
| `s` | プロジェクト全体の超高速全文検索（AND検索対応） | `consult-ripgrep` |
| `w` | カーソル下の単語で全文検索 | `my/consult-ripgrep-word` |
| `o` | 見出し（章・節・関数）一覧ジャンプ | `consult-outline` |
| `i` | ソースコードの関数・クラス・変数定義一覧 | `consult-imenu` |
| `m` | このファイルの足跡（マーク）履歴一覧 | `consult-mark` |
| `M` | 全ファイルの足跡（マーク）履歴一覧 | `consult-global-mark` |
| `'` | 現在位置をピン留め（足跡記録） | `my/quick-pin-set` |
| `c` | このファイルのピン（足跡）を全消去 | `my/quick-pin-clear` |
| `B` | 永続保存されたブックマーク一覧 | `consult-bookmark` |
| `y` | コピー履歴一覧から選んで貼り付け | `consult-yank-pop` |
| `t` | カラーテーマのリアルタイム試着・切り替え | `consult-theme` |
| `r` | レジスタ一覧 | `consult-register` |

---

## 置換（visual-replace）

> [!NOTE]
> リアルタイム置換パッケージとして、本環境では置換結果の「置換後」プレビューがより強力で直感的な **`visual-replace`** をメインに採用し、ツールバーに統合しています（`visual-regexp` もインストールされていますが、現在は使用していません）。

| キー / 操作 | 動作 |
|---|---|
| `M-%` | リアルタイム通常置換（プレビュー表示付き） ※デフォルトはバッファ全体 |
| `C-M-%` | リアルタイム正規表現置換（プレビュー表示付き） ※デフォルトはバッファ全体 |
| `C-h` | 置換オプション（通常/正規、全体/一部）のポップアップメニューを表示（ツールバーの`置換`ボタンと同じ） |
| ツールバーの `置換` ボタン | 置換オプション（通常/正規、全体/一部）のポップアップメニューを表示 |

### 置換入力中（ミニバッファ）のショートカット
| キー | 動作 |
|---|---|
| `C-r` / `M-r` | 正規表現モードの ON / OFF 切り替え（トグル） |
| `TAB` | 検索対象（From）と置換文字（To）の入力欄を切り替え |
| `RET` | 置換実行（1件ずつ確認画面へ移行） |
| `C-g` | 置換の中断 |

---

## フィルタ・grep編集（Occur / wgrep）

| キー | 動作 |
|---|---|
| `e` | (読み取り専用時) 編集モードに入る |
| `ESC` | (編集時) 編集モードを終了して読み取り専用に戻る |
| `C-c C-c` | 編集内容を確定して保存 (Occur: 元ファイルへ / wgrep: 全ファイルへ) |
| `C-c C-k` | (wgrep時) 編集内容を破棄して終了 |
| `q` | (読み取り専用時) バッファを閉じる |

---

## マルチカーソル（multiple-cursors）

| キー | 動作 |
|---|---|
| `C->` | 次の同じ単語にカーソル追加 |
| `C-<` | 前の同じ単語にカーソル追加 |
| `C-c m a` | バッファ内の全同一単語にカーソル追加 |
| `C-c m l` | 選択範囲の各行にカーソル追加 |
| `C-c m e` | 各行の行末にカーソル追加 |
| `RET` / `C-g` | マルチカーソル終了 |

---

## 矩形選択（rectangle-mark-mode）

| キー | 動作 |
|---|---|
| `Alt + ドラッグ` | マウスによる矩形選択を開始する |
| `C-RET` (Ctrl+Enter) | キーボードによる矩形選択を開始する |
| `ESC` | 矩形選択（または選択範囲）の解除 |

---

## カラーマーカー（symbol-overlay）

| キー | 動作 |
|---|---|
| `C-S-m` | マーカーをトグル（付ける / 外す） |
| `C-S-u` | 全マーカーを削除 |
| `C-S-n` | 次のマーカーへジャンプ |
| `C-S-p` | 前のマーカーへジャンプ |

---

## howm メモ

| キー | 動作 |
|---|---|
| `F8` / `M-o H` | howm 環境トグル（ON: メニュー / OFF: 全消去） |
| `C-c #` | カーソル下の #タグ で howm 検索 |
| `C-c v` | 画像を貼り付け（org-download） |

---

## Obsidian 連携

| キー | 動作 |
|---|---|
| `C-c o f` | Obsidian ファイルを開く |
| `C-c o i` | Obsidian リンクを挿入 |
| `C-c o c` | リンク先ファイルを新規作成 |

---

## ダッシュボード（emacs-dashboard）

ダッシュボード（`*dashboard*`）バッファが表示されているときに、以下のキーで直接機能にアクセスできます。

| キー | 動作 |
|---|---|
| `Home` | ダッシュボードを閉じて直前のバッファに戻る |
| `o` | メインメニュー（Hydra ランチャー） |
| `e` | Everything で PC 内検索 |
| `s` | プロジェクト内全文検索（ripgrep） |
| `g` | プロジェクト内ファイル名検索（fd） |
| `f` | 最近使ったファイルを開く |
| `c` | cmd.exe（ConPTY ターミナル） |
| `p` | PowerShell（ConPTY ターミナル） |
| `L` | カレンダー（calfw） |
| `d` | 辞書（Lookup） |
| `w` | ウィンドウ操作サブメニュー |
| `F` | ファイル操作サブメニュー |

---

## M-o ランチャー（Hydra）

`M-o` を押してから続けてキーを入力します。

### メインランチャー（M-o）

| キー | 動作 |
|---|---|
| `e` | Everything で PC 内検索 |
| `s` | プロジェクト内全文検索（ripgrep） |
| `g` | ファイル名検索（fd・プロジェクト全体） |
| `G` | ここから以下のファイル名検索（fd） |
| `f` | 最近使ったファイル |
| `z` | Zoxide でファイルを開く |
| `r` | ローマ字検索（Migemo） |
| `l` | カーソル下の単語をポップアップ検索 |
| `n` | 新しいフレームを開く |
| `d` | 辞書（Lookup） |
| `E` | EPUB リーダー（nov.el） |
| `H` | howm メモ ON/OFF トグル |
| `m` | Markdown サブメニュー → |
| `o` | Obsidian サブメニュー → |
| `p` | プロジェクトメニュー → |
| `w` | ウィンドウ操作サブメニュー → |
| `F` | ファイル操作サブメニュー → |
| `M` | カラーマーカーサブメニュー → |
| `b` | ブックマーク一覧（consult-bookmark） |
| `O` | バッファ一括検索・編集（moccur） |
| `L` | カレンダー（calfw） |
| `W` | 週間天気予報（中四国・全国） |
| `c` | cmd.exe（ConPTY） |
| `P` | PowerShell（ConPTY） |
| `C` | 電卓メニュー（calc） → |
| `T` | テキスト変換サブメニュー → |
| `q` | 閉じる |

### ウィンドウ操作（M-o w）

| キー | 動作 |
|---|---|
| `2` | 上下に分割 |
| `3` | 左右に分割 |
| `o` | 次のウィンドウへ移動 |
| `O` | 前のウィンドウへ移動 |
| `=` | 幅・高さをそろえる |
| `+` | 高さを広げる |
| `-` | 高さを縮める |
| `0` | このウィンドウを閉じる |
| `1` | 他のウィンドウをすべて閉じる |
| `k` | バッファを閉じる |
| `K` | バッファ＋ウィンドウを閉じる |

### ファイル操作（M-o F）

| キー | 動作 |
|---|---|
| `o` | ファイルを開く |
| `r` | 最近のファイル |
| `s` | 上書き保存 |
| `S` | 名前をつけて保存 |
| `k` | バッファを閉じる |
| `e` | 現在のファイルを外部アプリで開く |
| `E` | 任意のファイルを外部アプリで開く |
| `R` | 最近のファイルを外部アプリで開く |
| `m` | 音楽・動画をリアルタイム検索して再生（Everything / es.exe） |
| `a` | 現在のファイルを agy.exe に渡して cmd 外部窓で実行 |
| `A` | 現在のファイルを agy.exe に渡して PowerShell 外部窓で実行 |
| `x` | 任意の外部コマンドを cmd 外部窓で実行（%f=現在ファイルパス） |
| `X` | 任意の外部コマンドを PowerShell 外部窓で実行（%f=現在ファイルパス） |
| `d` | WinMerge で差分比較 |
| `c` | 文字コード指定で開き直す |
| `p` | Pandoc で別形式に変換 |
| `K` | EPUB を Kindle 形式に変換 |

### Markdown（M-o m）

| キー | 動作 |
|---|---|
| `1` | 見出し H1 挿入 |
| `2` | 見出し H2 挿入 |
| `b` | 太字（Bold）挿入 |
| `k` | 斜体（Italic）挿入 |
| `c` | コードブロック挿入 |
| `l` | リンク挿入 |
| `i` | 画像リンク挿入 |
| `t` | 目次（TOC）生成・更新 |
| `o` | 見出し検索ジャンプ（consult-outline） |
| `O` | アウトラインサイドバー開閉 |

### Obsidian（M-o o）

| キー | 動作 |
|---|---|
| `f` | ファイルを開く |
| `n` | 新規ノート作成 |
| `i` | リンクを挿入 |
| `c` | リンク先ファイルを作成 |
| `s` | Vault 内全文検索（ripgrep） |
| `r` | Vault 内ローマ字全文検索（Migemo） |
| `F` | Vault 内ファイル名検索（Migemo） |

### カラーマーカー（M-o M）

| キー | 動作 |
|---|---|
| `m` | マーカーをトグル |
| `u` | 全マーカーを削除 |
| `n` | 次のマーカーへ |
| `p` | 前のマーカーへ |
| `d` | 定義箇所へジャンプ |
| `c` | Casual メニュー |

### テキスト変換（M-o T）

| キー | 動作 |
|---|---|
| `s` | 選択行を昇順ソート |
| `S` | 選択行を降順ソート |
| `u` | 選択範囲の重複行を削除 |
| `z` | 選択範囲を半角 → 全角 |
| `Z` | 選択範囲を全角 → 半角 |
| `n` | 選択範囲に限定 (Narrow) |
| `w` | 限定を解除 (Widen) |

---

## EWW（内蔵Webブラウザ）

| キー | 動作 |
|---|---|
| `l` / `Backspace` | 戻る（前のページ） |
| `r` | 進む（次のページ） |
| `g` | ページの再読み込み（Reload） |
| `Tab` / `S-Tab` | 次 / 前のリンクへ移動 |
| `Enter` | リンクを開く |
| `M-Enter` | リンクを新しいタブ（別バッファ）で開く |
| `o` | 見出し目次ジャンプ（`consult-imenu` 連携） |
| `F4` | 目次サイドバー開閉（`imenu-list`） |
| `w` / `y` | Markdownリンクコピー（`[タイトル](URL)` 形式。`C-u w` でURLのみ） |
| `R` | リーダーモード（本文だけを抽出して広告・サイドバー全消去） |
| `i` | 画像表示 ON / OFF 切り替え（初期状態は爆速テキスト表示） |
| `C` | 配色切り替え（Webサイト指定色 ⇆ Emacsダークテーマ統一色） |
| `&` | 外部ブラウザで開く（Chrome / Edge で即座に開き直す） |
| `H` | 閲覧履歴検索（`consult` 連携でインクリメンタルジャンプ） |
| `b` | 現在のページをブックマーク登録 |
| `B` | ブックマーク一覧・検索（`consult` 連携でインクリメンタルジャンプ） |
| `S` | 開いている EWW バッファ一覧 |
| `C-w` | タブ（バッファ）を閉じる |
| `F1` / `?` | EWW 操作ガイド（Hydra）を表示 |

---

## nov.el（EPUB 電子書籍リーダー）

ヘッダーラインに書籍タイトル・章名・章番号（第 X/Y 章）・読書進捗率（%）が常時表示されます。

| キー | 動作 |
|---|---|
| `Space` / `n` | 次のページへスクロール |
| `Backspace` / `p` | 前のページへスクロール |
| `d` / `u` | 半画面進む / 戻る（快適な流し読み） |
| `j` / `k` | 1行進む / 戻る（スムーズスクロール） |
| `]` / `N` | 次の章へ進む |
| `[` / `P` | 前の章へ戻る |
| `m` | しおりを挟む（現在の読書位置を Bookmark に保存） |
| `b` | しおりへジャンプ（保存した位置へ移動） |
| `M` | しおり一覧（`consult-bookmark` でしおりを選択して即移動） |
| `B` | 電子書籍本棚（最近読んだ本や登録フォルダ内の書籍を一覧検索） |
| `o` | 全章目次ジャンプ（`consult-imenu` 連携で読みたい章へ即移動） |
| `F4` | 目次サイドバー開閉（`imenu-list`） |
| `t` | 目次ページへ移動 |
| `+` / `-` | 文字サイズ拡大 / 縮小 |
| `0` | 文字サイズリセット |
| `Tab` / `S-Tab` | リンク移動 |
| `Enter` | リンクを開く |
| `q` | リーダーを閉じる |
| `F1` / `?` | EPUB リーダー操作ガイド（Hydra）を表示 |

---

## カレンダー（calfw / howm 予定連携）

<kbd>F8</kbd> または <kbd>M-o L</kbd> でカレンダーを開閉（トグル）します。

| キー | 動作 |
|---|---|
| `i` / `a` | クイック予定追加（選択中の日付に howm 予定を直接登録し、画面を即座に再描画） |
| `c` | 詳細予定メモ作成（選択日付をタイトルにした Markdown メモを新規作成して編集） |
| `RET` | その日のメモ一覧（選択日付に関連する howm メモを検索して一覧表示） |
| `SPC` | 選択項目の詳細ポップアップ |
| `h` / `j` / `k` / `l` | 日付移動（左 下 上 右） |
| `←` / `↓` / `↑` / `→` | 日付移動（左 下 上 右） |
| `t` / `.` | 今日の日付へ移動 |
| `M-g` | 指定日付へジャンプ |
| `M` | 月表示（Month view） |
| `W` | 週表示（Week view） |
| `T` | 2週間表示（Two weeks view） |
| `D` | 日表示（Day view） |
| `g` | カレンダーを最新状態に再描画（リフレッシュ） |
| `F8` / `q` | カレンダーを閉じる（元の画面へ復帰） |
| `F1` / `?` | カレンダー操作ガイド（Hydra）を表示 |

---

## 週間天気予報（中四国・全国）

<kbd>M-o W</kbd> または `M-x weather` で気象庁公式データによる週間天気予報テーブルを表示します（Python不要・高速取得）。

| キー | 動作 |
|---|---|
| `r` | 最新の気象庁データに再取得・更新（リフレッシュ） |
| `q` / `ESC` | 天気予報バッファを閉じる |
